<?xml version="1.0" ?><!-- Cyberprobe configuration file. --><configuration>

  <!-- Define network interfaces to sniff. -->
  <interfaces>
    <!-- <interface name="eth0"/> -->
  </interfaces>

  <!-- Define IP addresses or address ranges to match on. -->
  <targets>
    <!-- <target address="192.168.0.0/24" liid="my-network"/> -->
  </targets>

  <!-- Define endpoints to deliver data to. -->
  <endpoints>
    <endpoint certificate="cert.probe" host="probe-svc.ops.trustnetworks.com" key="key.probe" port="9001" transport="tls" trusted-ca="cert.ca" type="etsi"/>
  </endpoints>

</configuration>